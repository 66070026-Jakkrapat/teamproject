# Vercel deployment: OpenAI API + optional RAG
fastapi
uvicorn[standard]
python-dotenv
requests
httpx
openai>=1.0
pypdf

# Database (optional — needed for RAG queries on Vercel)
sqlalchemy>=2.0
asyncpg
pgvector
