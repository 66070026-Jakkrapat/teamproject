# Lightweight dependencies for Vercel deployment
# Heavy ML packages (torch, paddleocr, transformers, etc.) are excluded
# because they exceed Vercel's 500 MB Lambda limit.
# Heavy processing is offloaded to a separate worker.

fastapi
uvicorn[standard]
python-dotenv
requests
httpx
beautifulsoup4
numpy

# Database (required for RAG queries on Vercel)
sqlalchemy>=2.0
asyncpg
pgvector
