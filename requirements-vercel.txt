# Lightweight dependencies for Vercel deployment
# Uses OpenAI API instead of local models

fastapi
uvicorn[standard]
python-dotenv
requests
httpx
openai>=1.0

# Database (optional — needed for RAG queries on Vercel)
sqlalchemy>=2.0
asyncpg
pgvector
